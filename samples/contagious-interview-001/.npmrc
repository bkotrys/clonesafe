# Safety: prevent lifecycle scripts from running if someone accidentally
# runs `npm install` in this sample directory.
# The package.json in this directory has a malicious "prepare" hook
# that would attempt to launch a background server. This .npmrc
# ensures that hook is never executed.
ignore-scripts=true
